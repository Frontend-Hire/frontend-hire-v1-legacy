import { Callout } from '@/components/MarkdownComponents';
import { HowToRunHTML } from './examples/how-to-run-html';

export const meta = {
  lastUpdated: '2024-08-15'
}

# How to run HTML?

We will cover how to set up a developer environment at the end of the cohort. For now, you can just focus on writing the HTML code in our interactive exercises.

## Example of an Interactive Exercise

Make any changes to the code and it will instantly update the preview.

For example, try to update the text in the `h1` tag to your name.

<HowToRunHTML />

<Callout>
Do not worry about what the HTML code above does, we will cover all that in the next few chapters.
</Callout>