import { Callout } from '@/components/MarkdownComponents';

export const meta = {
  lastUpdated: '2024-08-15'
}

# HTML V1

At the core, HTML is simple and you can learn most of it in less than a week. We will cover all the HTML you would need on a day-to-day basis. Anything that is not covered can easily be looked up on the web.

Also, this is a very practical cohort with interactive exercises. We won't discuss historical theory or things that are trivia. Our goal is to know just enough HTML to move on to CSS. 

Here's a quick overview of the cohort:

- How to run HTML?
- Headings